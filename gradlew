#!/usr/bin/env sh

# Minimal gradlew stub. If you use Android Studio, it will usually regenerate a full wrapper.
# Recommended: run `gradle wrapper` once on your machine to generate the standard scripts and wrapper JAR.

echo "This project zip includes gradle-wrapper.properties only. Generate wrapper scripts/jar locally (Gradle -> wrapper)."
exit 1
